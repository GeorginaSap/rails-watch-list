<%= simple_form_for @list do |form| %>
  <div class="form-group">
    <%= form.input :name, label: "Name of the List" %>
  </div>
  <div class="form-group">
    <%= form.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>
